import { IconButton } from "@material-ui/core";
import React from "react";

const DrawerIcon = () => {
  const handleDrawerClose = () => {};

  return <IconButton onClick={handleDrawerClose}></IconButton>;
};

export default DrawerIcon;
